spring boot backend and an angular frontend with mysql as db all is localhost and the backend is supposed to have input validation for fields, it should follow mvc architecture with the following sorted files dao,dto,entity,repo,service impl,service,controller,mapper. It aslo has to allow for CORS to link to angular frontend and the db has to be made dynamically using JPArepository. The app is fintech/financial bbank app so it has to have good validation and error handling the Frontend need not be too fancy[if tokend/credits are avialable after makign backend only then try to make  a beautiful frontend]  we have to follow insert only paradigm so that deleted cols have D in CRUD field , when updated U and the newset updated row has to sent, the endpoints should be simple such as for a entity CustomerDetail it should just be /detail the backend has to be RESTFUL with get put post  delete, entity has to have all the field with audit fields whereas the dto has to have only required fields that we can show to user, the services for each entity also has to have support for pagination and the angular frontend has to be with simple css if less credits/topkens left, the bakcend also has to have soem endpoints to do bulk customergenertaion schema wide with /files for accepting json csv excel /gen/{count} for generating count no of random customers and /fill/{count} to use standard arrays and a random picker to craft count entries the app. The mapper has to use the java mapper package, and the response can be in the form of code blocks with as efficient/tight line spacing, avoid comments and giving explainations the code output has to be given simply, if possible send a zip file else just code blocks for each file....avoid truncating the code ,only truncate the uneeded comments and spacing and new lines, also ensure the package fxn exist in the current version of java and for debugging ensure that instead of giving full files give a NUMBER line position and the code snippet where the changes have to be made avoid formatting the text like headers and stuff avoid anything that isnt necessary liek formating prints etc unless its the frontend


spring boot backend and an angular frontend with mysql as db all is localhost and the backend is supposed to have input validation for fields, it should follow mvc architecture with the following sorted files dao,dto,entity,repo,service impl,service,controller,mapper. It aslo has to allow for CORS to link to angular frontend and the db has to be made dynamically using JPArepository. The app is fintech/financial bbank app so it has to have good validation and error handling the Frontend need not be too fancy[if tokend/credits are avialable after makign backend only then try to make  a beautiful frontend]  we have to follow insert only paradigm so that deleted cols have D in CRUD field , when updated U and the newset updated row has to sent, the endpoints should be simple such as for a entity CustomerDetail it should just be /detail the backend has to be RESTFUL with get put post  delete, entity has to have all the field with audit fields whereas the dto has to have only required fields that we can show to user, the services for each entity also has to have support for pagination and the angular frontend has to be with simple css if less credits/topkens left, the bakcend also has to have soem endpoints to do bulk customergenertaion schema wide with /files for accepting json csv excel /gen/{count} for generating count no of random customers and /fill/{count} to use standard arrays and a random picker to craft count entries the app. The mapper has to use the java mapper package, and the response can be in the form of code blocks with as efficient/tight line spacing, avoid comments and giving explainations the code output has to be given simply, if possible send a zip file else just code blocks for each file....avoid truncating the code ,only truncate the uneeded comments and spacing and new lines, also ensure the package fxn exist in the current version of java and for debugging ensure that instead of giving full files give a NUMBER line position and the code snippet where the changes have to be made avoid formatting the text like headers and stuff avoid anything that isnt necessary liek formating prints etc unless its the frontend









































